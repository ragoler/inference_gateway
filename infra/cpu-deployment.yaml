apiVersion: apps/v1
kind: Deployment
metadata:
  name: vllm-cpu-server
spec:
  replicas: 2
  selector:
    matchLabels:
      app: vllm-cpu-server
  template:
    metadata:
      labels:
        app: vllm-cpu-server
        inference.networking.k8s.io/engine-type: vllm
    spec:
      containers:
      - name: vllm
        image: "vllm/vllm-openai-cpu:latest"
        imagePullPolicy: Always
        command: ["python3", "-m", "vllm.entrypoints.openai.api_server"]
        args:
        - "--model"
        - "${MODEL_NAME}"
        - "--port"
        - "8000"
        - "--max-model-len"
        - "4096"
        # Publish KV-cache events over ZMQ so an llm-d-style indexer can track
        # exactly which prefix blocks live on which pod (event-driven routing).
        - "--kv-events-config"
        - '{"enable_kv_cache_events":true,"publisher":"zmq","endpoint":"tcp://*:5557","topic":"kv@$(POD_IP):5557@${MODEL_NAME}"}'
        env:
        - name: PORT
          value: "8000"
        # Cap the CPU KV Cache to avoid OOMKilled errors
        - name: VLLM_CPU_KVCACHE_SPACE
          value: "${KV_CACHE_SPACE}"
        - name: POD_IP
          valueFrom:
            fieldRef:
              fieldPath: status.podIP
        ports:
        - containerPort: 8000
          name: http
          protocol: TCP
        - containerPort: 5557
          name: kv-events
          protocol: TCP
        livenessProbe:
          failureThreshold: 240
          httpGet:
            path: /health
            port: http
            scheme: HTTP
          initialDelaySeconds: 5
          periodSeconds: 5
          successThreshold: 1
          timeoutSeconds: 1
        readinessProbe:
          failureThreshold: 600
          httpGet:
            path: /health
            port: http
            scheme: HTTP
          initialDelaySeconds: 5
          periodSeconds: 5
          successThreshold: 1
          timeoutSeconds: 1
        resources:
          limits:
            cpu: "4"
            memory: "20Gi"
            ephemeral-storage: "10Gi"
          requests:
            cpu: "4"
        volumeMounts:
        - mountPath: /dev/shm
          name: shm
      volumes:
      - name: shm
        emptyDir:
          medium: Memory
          sizeLimit: 4Gi
      restartPolicy: Always
